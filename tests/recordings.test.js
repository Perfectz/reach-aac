import test from 'node:test';import assert from 'node:assert/strict';import {RecordingLibrary,recordingKey} from '../src/recordings.js';
test('a saved clip must match both exact message text and language',()=>{const lib=new RecordingLibrary();lib.items.set(recordingKey('yes','en'),{text:'Yes',blob:new Blob(['audio'])});assert.ok(lib.get('yes','en','Yes'));assert.equal(lib.get('yes','en','No'),null);assert.equal(lib.get('yes','th','Yes'),null);assert.equal(lib.get('no','en','Yes'),null);});
test('recording storage rejects empty, non-audio and oversized clips before writing',async()=>{const lib=new RecordingLibrary();lib.person='person';for(const blob of [new Blob([],{type:'audio/webm'}),new Blob(['x'],{type:'text/html'}),new Blob([new Uint8Array(2_000_001)],{type:'audio/webm'})])await assert.rejects(()=>lib.save({phrase:'yes',language:'en',text:'Yes',blob}),/30 seconds/);});
