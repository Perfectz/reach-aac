import test from 'node:test';import assert from 'node:assert/strict';import {editPhrase} from '../src/phrase-editor.js';import {exactTranslation} from '../src/partner.js';import {validateSettings} from '../src/access.js';
test('phrase edits keep stable IDs and native-language fallback without inventing English',()=>{const original={id:'custom-stable',en:'Old',icon:'MessageCircle',tone:'neutral'},edited=editPhrase(original,{ar:' أحبك '},'ar');assert.equal(edited.id,original.id);assert.equal(original.en,'Old');assert.equal(edited.en,'أحبك');assert.equal(exactTranslation(edited,'en'),null);assert.equal(exactTranslation(edited,'ar'),'أحبك');assert.equal(validateSettings({phrases:[edited]}).phrases[0].sourceLanguage,'ar');});
test('empty source text is rejected and removed translations do not survive in fallback fields',()=>{assert.throws(()=>editPhrase({id:'custom-a'},{th:'ข้อความ'},'en'),/original language/);const result=editPhrase({id:'custom-a',en:'Before',th:'ก่อน',translations:{th:'ก่อน'}},{en:'After'},'en');assert.equal(exactTranslation(result,'th'),null);assert.equal(result.en,'After');});
